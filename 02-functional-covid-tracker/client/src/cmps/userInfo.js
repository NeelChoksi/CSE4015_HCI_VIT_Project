import React from "react";
const userInfo = ()=>{
    return(
        <div>
            fetch all users information and see : state of user, vaccine info , rtpcr ,latest tempox

            <p>take input for dailytempox, take input for vaccine , take input for rtpcr</p>
        </div>
    )
}

export default userInfo;